  <%= simple_form_for [@colony, @booking] do |f| %>

    <div class="form-inputs">
      <%= f.input :start, as: :date, html5: true %>
      <%= f.input :end, as: :date, html5: true %>

      <%= f.submit "Book now!" %>
    </div>
  <% end %>
